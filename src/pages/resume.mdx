---
layout: ../layouts/BaseLayout.astro
title: Résumé
description: 'Résumé of Gagan Jain, AI / LLM Engineer'
---

import { SITE_CONFIG } from '../data/config.ts'
import { PROJECTS, getTagClasses } from '../data/projects.ts'
import { EXPERIENCE } from '../data/experience.ts'
import ProjectLinkButton from '../components/ProjectLinkButton.astro'
import PrimaryButton from '../components/PrimaryButton.astro'
import Icon from '../components/Icon.astro'
import Pill from '../components/Pill.astro'

export const SKILL_GROUPS = [
  {
    name: 'AI / LLM',
    skills:
      'LLM Integration, RAG Pipelines, Agentic AI Frameworks, Prompt Engineering, Context Engineering, Multi-Agent Systems, Tool-Using Agents, Fine-tuning (QLoRA/LoRA), MCP / Tool Protocols, LangChain / LlamaIndex, Vector Databases, AI Governance',
  },
  {
    name: 'LLM Ops',
    skills:
      'Evaluation & Observability, Local LLM Deployment, OpenAI / Anthropic APIs, Embeddings, Structured Outputs / Function Calling, LiteLLM / Model Routing, Streaming Responses, NLP, Ollama',
  },
  {
    name: 'Languages',
    skills: 'Python, TypeScript, JavaScript, Rust, C++, C, PHP, HTML/CSS',
  },
  {
    name: 'Backend & APIs',
    skills: 'Flask, FastAPI, Node.js, Express, Laravel, Axum (Rust), REST APIs, WebSockets',
  },
  {
    name: 'Databases & DevOps',
    skills:
      'MySQL, MongoDB, Redis, SQLite, Supabase, Docker, GitHub Actions, Nginx, Gunicorn, Prometheus, Linux',
  },
  {
    name: 'Frontend & Vision',
    skills:
      'React, React Native, Next.js, Tailwind CSS, Bootstrap, Monaco Editor, YOLO, OpenCV, Computer Vision',
  },
]

export const SOCIAL_LINKS = [
  { href: 'https://linkedin.com/in/gaganjainse', label: 'linkedin.com/in/gaganjainse' },
  { href: 'https://github.com/gaganjainse', label: 'github.com/gaganjainse' },
  { href: 'https://gaganjain.vercel.app', label: 'gaganjain.vercel.app' },
]

<div class="max-w-4xl mx-auto px-4 sm:px-6 pt-20 sm:pt-24 pb-16 resume-page">
  <div class="glow-card relative rounded-xl shadow-2xl shadow-black/20 bg-bg-card/80 backdrop-blur-sm p-6 sm:p-10 resume-sheet">
    <a
      href="/resume.pdf"
      class="no-print group absolute top-4 right-4 sm:top-6 sm:right-6 inline-flex items-center justify-center w-11 h-11 rounded-xl bg-primary text-white overflow-hidden transition hover:shadow-lg hover:shadow-primary/25 hover:-translate-y-0.5"
      aria-label="Download résumé as PDF"
      title="Download PDF"
    >
      <span class="relative z-10">
        <Icon name="download" class="w-5 h-5" />
      </span>
      <span
        class="absolute inset-0 bg-gradient-to-r from-primary-light to-accent opacity-0 group-hover:opacity-100 transition-opacity"
        aria-hidden="true"
      ></span>
    </a>
  <div class="text-center mb-10">
    <h1 class="text-3xl sm:text-4xl font-bold mb-3 gradient-text resume-name">Gagan Jain</h1>
    <p class="text-lg text-text-muted mb-2 resume-role">AI / LLM Engineer</p>
    <p class="text-text-muted resume-contact">{SITE_CONFIG.location} (Open to Relocation) &middot; {SITE_CONFIG.phone} &middot; <a href={`mailto:${SITE_CONFIG.email}`} class="text-accent hover:underline">{SITE_CONFIG.email}</a></p>
    <div class="flex flex-wrap items-center justify-center gap-3 mt-3 text-sm resume-social">
      {
        SOCIAL_LINKS.map((link, i) => (
          <span key={i} class="inline-flex items-center gap-1">
            {i > 0 && <span class="text-border">·</span>}
            <a
              href={link.href}
              target="_blank"
              rel="noopener noreferrer"
              class="text-accent hover:underline inline-flex items-center gap-1"
            >
              {link.label}
              <Icon name="external" class="w-3.5 h-3.5 opacity-80" />
            </a>
          </span>
        ))
      }
    </div>
  </div>

<div class="resume-divider" aria-hidden="true"></div>

<section class="mb-10 scroll-mt-24">
  <h2 class="text-xl font-bold gradient-text mb-3">Professional Summary</h2>
  {/* prettier-ignore */}
  <p class="text-text-muted leading-relaxed">AI Engineer focused on production-grade GenAI systems: agentic AI orchestration, RAG pipelines, LLM fine-tuning (QLoRA/LoRA), MCP tooling, and local-first LLM deployment. Author of NexusAOS, a governance-first, event-sourced AI operating environment written in Rust (12 workspace crates, 981 tests, full CI/CD) with provider-agnostic LLM streaming (OpenAI/Anthropic), policy-enforced agent actions, and terminal/SSH integration. Strong Python + Rust foundation with end-to-end deployment: Docker, GitHub Actions, observability, and AI governance.</p>
</section>

<div class="resume-divider" aria-hidden="true"></div>

<section class="mb-10 scroll-mt-24">
  <h2 class="text-xl font-bold gradient-text mb-3">Technical Skills</h2>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {SKILL_GROUPS.map((group) => (
      <div class="glow-card rounded-xl border border-border p-5 resume-card">
        <h3 class="text-base font-semibold text-text mb-2">{group.name}</h3>
        <p class="text-sm text-text-muted leading-relaxed">{group.skills}</p>
      </div>
    ))}
  </div>
</section>

<div class="resume-divider" aria-hidden="true"></div>

<section class="mb-10 scroll-mt-24">
  <h2 class="text-xl font-bold gradient-text mb-4">Projects</h2>
  <div class="space-y-5">
    {PROJECTS.map((project) => (
      <div class="glow-card rounded-xl border border-border p-5 resume-card">
        <div class="flex flex-wrap items-center gap-2 mb-2">
          <h3 class="text-lg font-bold text-text">{project.title}</h3>
          <span
            class={`px-2 py-0.5 rounded-full text-xs font-bold ${getTagClasses(project.tagColor)} cursor-default`}
          >
            {project.tag}
          </span>
        </div>
        <p class="text-text-muted text-sm mb-2">{project.description}</p>
        <ul class="list-disc list-inside space-y-1 text-sm text-text-muted mb-3">
          {project.bullets.map((bullet) => (
            <li>{bullet}</li>
          ))}
        </ul>
        <div class="flex flex-wrap items-center gap-3 mt-3">
          <ProjectLinkButton href={project.github} label="Source Code" icon="github" external />
          {project.docs && (
            <ProjectLinkButton
              href={`${SITE_CONFIG.siteUrl}${project.docs}`}
              label="Documentation"
              icon="doc"
            />
          )}
        </div>
      </div>
    ))}
  </div>
</section>

<div class="resume-divider" aria-hidden="true"></div>

<section class="mb-10 scroll-mt-24">
  <h2 class="text-xl font-bold gradient-text mb-4">Experience</h2>
  <div class="timeline">
    {EXPERIENCE.map((job) => (
      <div class="glow-card timeline-item rounded-xl border border-border p-5 resume-card">
        <div class="flex flex-wrap items-start justify-between gap-3 mb-2">
          <div>
            <h3 class="text-lg font-bold text-text">{job.title}</h3>
            <p class="text-primary-light font-medium">{job.company}</p>
          </div>
          <Pill label={job.period} variant="period" />
        </div>
        {job.location && <p class="text-text-muted text-sm mb-2">{job.location}</p>}
        <ul class="list-disc list-inside space-y-1 text-sm text-text-muted">
          {job.bullets.map((bullet) => (
            <li>{bullet}</li>
          ))}
        </ul>
      </div>
    ))}
  </div>
</section>

<div class="resume-divider" aria-hidden="true"></div>

  <section class="scroll-mt-24">
    <h2 class="text-xl font-bold gradient-text mb-4">Education</h2>
    <div class="glow-card rounded-xl border border-border p-5 resume-card">
      <div class="flex flex-wrap items-start justify-between gap-3">
        <div>
          <h3 class="text-lg font-bold text-text">Vellore Institute of Technology, Vellore</h3>
          <p class="text-primary-light font-medium">B.Tech in Computer Science and Engineering</p>
          <p class="text-text-muted text-sm mt-1">{/* prettier-ignore */}CGPA: 7.7 / 10.0 | Relevant Coursework: Data Structures &amp; Algorithms, Operating Systems, Database Systems, Computer Networks, Web Technologies, Artificial Intelligence, Machine Learning</p>
        </div>
        <Pill label={`Graduated ${SITE_CONFIG.graduation}`} variant="period" />
      </div>
    </div>
  </section>
  </div>
</div>
