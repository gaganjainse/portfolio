---
layout: ../../../layouts/DocsLayout.astro
title: SheshAOS
description: 'SheshAOS project documentation'
---

import TechChip from '../../../components/TechChip.astro'

<h1 class="text-4xl font-bold mb-6 gradient-text">SheshAOS</h1>
{/* prettier-ignore */}
<p class="text-text-muted mb-8">Governance-first, event-sourced AI operating environment written in Rust — 9 workspace crates + CLI, 877+ passing tests, full CI/CD.</p>

<div class="gradient-border p-6 mb-6">
  <h2 class="text-2xl font-bold text-text mb-3">Overview</h2>
  {/* prettier-ignore */}
  <p class="text-text-muted mb-4">A governance-first AI OS where LLMs propose actions and the kernel validates and records every state change in an append-only audit trail. Provider-agnostic streaming (OpenAI-compatible + Anthropic) with local-first inference that runs fully offline.</p>
</div>

<div class="gradient-border p-6 mb-6">
  <h2 class="text-2xl font-bold text-text mb-3">Architecture</h2>
  {/* prettier-ignore */}
  <p class="text-text-muted mb-4">9 workspace crates plus the <code>shesh</code> CLI: kernel, wave-object store, workspace processes, block control, AI layer, remote/SSH, RPC, vault, and config. LiteLLM routing, native terminal emulation (PTY + VT100), SSH multiplexing, and four interfaces: CLI, TUI, GUI, RPC.</p>
</div>

<div class="gradient-border p-6">
  <h2 class="text-2xl font-bold text-text mb-3">Tech Stack</h2>
  <div class="flex flex-wrap gap-2">
    {['Rust', 'Tokio', 'Event Sourcing', 'LiteLLM', 'OpenAI', 'Anthropic', 'SSH', 'PTY'].map(
      (tech) => (
        <TechChip label={tech} />
      ),
    )}
  </div>
</div>
